require "rails_helper"

RSpec.describe LighthouseMailer, type: :mailer do
  # # Suponiendo que tienes una fábrica para User
  # let(:user) { create(:user) }
  # let(:mail) { LighthouseMailer.some_mail(user) }

  # it 'queues the email' do
  #   expect { mail.deliver_now }.to change { ActionMailer::Base.deliveries.count }.by(1)
  # end

  # it 'renders the headers' do
  #   expect(mail.subject).to eq('Expected subject')
  #   expect(mail.to).to eq([user.email])
  #   expect(mail.from).to eq(['from@example.com'])
  # end

  # it 'renders the body' do
  #   expect(mail.body.encoded).to match('Expected email body')
  # end
end